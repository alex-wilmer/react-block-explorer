@import 'nib'

global-reset()

*
  box-sizing border-box

.tx + .tx
  margin-top 15px

.high-roller
  color #00B300
  font-weight bold
  font-size 1.1em

for num in (1..10)
  .fade-{num}
    opacity (1 - (num / 10))
